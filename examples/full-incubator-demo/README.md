# full-incubator-demo

Demo completa de la incubadora virtual usando `firmware.bin` de la placa madre y todos los custom chips.

## Ejecutar

1. Desde la raíz del repo, ejecuta `./tools/build-chips.sh`.
2. Abre esta carpeta en VS Code.
3. Ejecuta `Wokwi: Start Simulator`.
